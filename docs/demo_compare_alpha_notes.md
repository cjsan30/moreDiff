# Demo Compare Alpha Notes

This branch simulates a UI-focused follow-up change.

- branch: `codex/agent-ui/demo-compare-alpha`
- changed area: docs and shared compare target
- use case: verify that MoreDiff can show one branch-specific file plus a shared-file overlap
